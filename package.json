{
  "name": "becanine",
  "version": "0.1.0",
  "private": true,
  "description": "BE Canine — premium botanical canine nutrition supplements. One-page storefront for becanine.com.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "wrangler:dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@fontsource/eb-garamond": "^5.2.7",
    "@fontsource/im-fell-english": "^5.2.6",
    "@fontsource/im-fell-english-sc": "^5.2.6",
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
